import XCTest
@testable import MaterialForm

final class MaterialFormTests: XCTestCase {

    func testExample() {
        // This test does nothing, as there is nothing to test now
        XCTAssertTrue(true, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
